use anyhow::Error;
use vulkanalia::vk;
use vulkanalia::vk::HasBuilder;

struct RenderPass {}

impl RenderPass {
    pub fn new() -> Result<Self, Error> {
        // continued...

        Ok(Self {

        })
    }
}